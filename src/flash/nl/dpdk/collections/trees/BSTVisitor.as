package nl.dpdk.collections.trees {	import nl.dpdk.collections.lists.LinkedList;	import nl.dpdk.collections.lists.List;	/**	 * helper class that stores the BST in a list	 */	internal class BSTVisitor implements ITreeNodeVisitor {		private var list : LinkedList = new LinkedList();		public function visit(node : ITreeNode) : void {			list.add(node.getData());		}				public function getList() : List {			return list;			}	}}